/**
 * Minimal enhancements — no tracking by default.
 * Add analytics snippet in layout when ready.
 */
(function () {
  document.documentElement.classList.add("js-ready");

  // Smooth in-view subtle fade could be added later
})();
